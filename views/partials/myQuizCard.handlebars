 <div class="col p-3">
    <div class="card bg-dark bg-blue-crayola text-black" style="border-radius: 1rem;">
    <div class="card-body p-5 text-center">
        <div class=" mb-md-5 mt-md-4 pb-5">
        <h2 class="fw-bold fs-4 fs-md-3 fs-lg-2 mb-2 text-uppercase">{{dataValues.title}}</h2>
        <p class="text-black mb-5">{{dataValues.description}}</p>
        {{!-- ADD THE WAY IT WILL SELCT THE CORRECT QUIZ BASED ON WHAT IS PRESSED --}}
        <a class="btn btn lg btn-dark" id="deleteBtn" data-quiz="{{dataValues.id}}">Remove</a>
        </div>
    </div>
    </div>
</div>